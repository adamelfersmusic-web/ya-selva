---
status: open
date: 2026-08-30
---

# 000_CANON — Ya Selva

> # ⛔ NOT RATIFIED. THIS IS A DRAFT.
>
> **Adam did not write this file. Claude assembled it on 2026-08-30 because
> `000_CANON.md` was named as the anchor for this repo and does not exist in it —
> not on `main`, not on any branch, not anywhere in the commit history.**
>
> **Nothing below is LOCKED until Adam says it is.** Every line here was read off
> what the repo already asserts about itself, not decided. The two LOCKED items
> are locked because the files themselves say `status: locked` — inherited, not
> ruled.
>
> **If the real CANON exists somewhere else, that one wins and this file should be
> superseded on sight.** It was probably in `Ya selva context.zip`, which never
> reached this repo.

---

## How to read this file

`CANON` is the only file that states decision state. Anything in this repo that
contradicts a LOCKED item here is out of date; CANON wins until Adam changes it
deliberately.

```
LOCKED     decided. Do not re-derive. Do not re-open without saying so.
WORKSHOP   in progress. Gets locked by being RUN, not by being discussed.
OPEN       a real unresolved question.
CAPTURE    raw, in Adam's voice, append-only, never canonical.
```

---

## 🔒 LOCKED

Two items. Both inherited from files that declare their own locked status.

### L1 · The master vision
**→ [`vision/master-vision.md`](vision/master-vision.md)**

> A moving musical center small enough to travel anywhere, porous enough to become
> larger through every place it touches.

Ya Selva carries the spine — songs, rig, sonic language, participatory method,
identity. The world supplies the body — musicians, dancers, voices, rhythms, rooms,
streets, field sounds. **Same sound. Different body.**

The bridge to Break This Loop is the distributed ensemble: *Ya Selva carries the
spine, the ensemble lives in the world.* Ya Selva encounters the world; Break This
Loop lets the world answer.

### L2 · The five guardrails
**→ [`vision/five-guardrails.md`](vision/five-guardrails.md)**

> Do not design a portable version of a big show. Design a tiny complete show that
> the world can expand.

1. The core show must work completely alone.
2. Expansion adds; it never rescues.
3. Carry the spine, source the body.
4. Never let production requirements kill mobility.
5. Every scale is the same organism.

---

## 🔧 WORKSHOP

Everything else in this repo. **Under Adam's own rule — workshop items get locked
by being RUN — nothing here can be locked yet, because no night has happened.**

| item | where |
|---|---|
| The night — run of show, capture protocol | [`live/the-night.md`](live/the-night.md) |
| UNDERPASS — the secret pop-up | [`live/underpass.md`](live/underpass.md) |
| The facilitator map | [`live/the-map.md`](live/the-map.md) · [`the-map.html`](the-map.html) |
| The distributed ensemble | [`live/the-distributed-ensemble.md`](live/the-distributed-ensemble.md) |
| The rig — 8 docs, tiers 0–5, the field bag | [`docs/rig/`](docs/rig/) |
| Character — north star, archetype, references | [`docs/character/`](docs/character/) |
| Ya Selva ⟷ BTL — connection, shared DNA, consent rule | [`docs/connection/`](docs/connection/) |
| Brand — design system, visual style, studio fabric | [`docs/brand/`](docs/brand/) |
| Ableton AI agent — idea stage, not built | [`docs/explorations/`](docs/explorations/) |

---

## ❓ OPEN

| # | question | from |
|---|---|---|
| O1 | **Is a room booked?** No date, host or venue appears anywhere in this repo. | see Active front |
| O2 | Rig proving phase — Move vs Push Standalone vs laptop+Push. Prove the behavior before buying anything. | [`docs/rig/one-rig-that-scales.md`](docs/rig/one-rig-that-scales.md) |
| O3 | "The future is ancient" — a Ya Selva line, a BTL line, or shared? | [`docs/connection/the-shared-dna.md`](docs/connection/the-shared-dna.md) |
| O4 | The participation gap — there is not one other human being in any current brand image. | [`docs/connection/the-shared-dna.md`](docs/connection/the-shared-dna.md) |
| O5 | Does "participatory music" replace "world music" in how Adam describes his own taste? | [`docs/connection/the-shared-dna.md`](docs/connection/the-shared-dna.md) |
| O6 | The pre-rule BTL corpus — off-limits for Ya Selva releases, or usable with full BTL-equivalent credit? | [`docs/connection/shared-raw-material.md`](docs/connection/shared-raw-material.md) |
| O7 | Does the consent line get written down — a sentence he says, or one on a phone screen he shows people? | [`docs/connection/shared-raw-material.md`](docs/connection/shared-raw-material.md) |
| O8 | What lives in the Parachute vault vs. this repo? | [`docs/_open-questions.md`](docs/_open-questions.md) |

---

## 🎯 ACTIVE FRONT

**Nothing is booked.**

Seven days of commits — 2026-08-24 through 2026-08-30 — are posters, docs, a
fabric spec, and two repo reorganizations. No commit, doc, or note in this repo
names a date, a host, or a room.

The repo's own connection doc already says it:

> Everything else is hypothesis until a night exists.

**This is the standing check firing.** Structure work is not the front. The front
is one host, one date, one room.

---

## 👉 NEXT ACTIONS

| # | action | blocked on |
|---|---|---|
| 1 | **Book one room.** One host, one date, this fall. | nothing — this is the front |
| 2 | Talk to Ayman | nothing |
| 3 | Make the map — city · facilitator · relationship · what they run · room size · drive time | nothing; the tool exists |
| 4 | Build the grab-and-go field bag; make the ready state the default state | nothing |
| 5 | Cut the trailer — ~90 sec, existing footage | nothing |

---

## ⚠️ NEEDS YOUR RULING — structural

The directory list Adam gave is `vision/ method/ songs/ live/ market/ study/
writing/ capture/`. Three placed themselves by exact name match:

```
vision/    ✅ placed — 4 docs
live/      ✅ placed — 4 docs
capture/   ✅ placed — 1 raw capture, append-only
method/    ⬜ empty
songs/     ⬜ empty
market/    ⬜ empty
study/     ⬜ empty
writing/   ⬜ empty
```

Five groups are still sitting under `docs/` because **CANON's index is the thing
that says where they go, and CANON was missing.** Guesses, not decisions:

| currently at | plausible home | why it's ambiguous |
|---|---|---|
| `docs/rig/` (8 docs) | `method/` | rig is *how the work gets made* — but it could be its own thing |
| `docs/character/` (4 docs) | `vision/`? | no `character/` in the list at all |
| `docs/connection/` (7 docs) | `market/`? `vision/`? | BTL relationship is both positioning and identity |
| `docs/brand/` (4 docs) | `market/`? | no `brand/` in the list; brand is not obviously market |
| `docs/explorations/` (1 doc) | `study/` | closest fit, still a guess |

**Also unanswered:** does `docs/` survive at all, or does everything move up?
`songs/` and `writing/` are empty — is that a gap to fill or just not yet?

---

## Conventions

- **Frontmatter on every file:** `status` (locked|workshop|open|capture), `date`,
  and `supersedes` if it replaces something.
- **Never delete.** Superseded files get a banner at the top pointing to the
  replacement, and their status changed.
- **`capture/` is append-only.** Raw dumps in Adam's voice. Never edited, never
  summarized in place, never cleaned up.
- **Source of truth is the Parachute vault** for content — `music/ya-selva/*` and
  `break-this-loop/*`. This repo holds artifacts. CANON is the exception: decision
  state lives here.
